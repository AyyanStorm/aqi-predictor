openmeteo-requests
requests-cache
retry-requests
tzdata  # IANA tz database for zoneinfo — Windows Python ships none, so
        # ZoneInfo() would raise ZoneInfoNotFoundError there (Day 24)
pandas
numpy
python-dotenv
pyarrow
scikit-learn
lightgbm  # Day 12 — production model (was missing from requirements!)
keras  # Day 15 LSTM baseline (runs on torch backend, no TensorFlow)

# --- Serving layer (Render deployment, Days 17-23) ---
streamlit
fastapi
uvicorn
plotly
streamlit-js-eval  # browser geolocation
shap  # Day 20 explainability
hopsworks  # Hopsworks feature-store client (Day 7 adapter; enabled now that
           # AYYAN's cluster + API key exist)
