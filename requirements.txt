openmeteo-requests
requests-cache
retry-requests
tzdata  # IANA tz database for zoneinfo — Windows Python ships none, so
        # ZoneInfo() would raise ZoneInfoNotFoundError there (Day 24)
pandas
numpy>=2.0,<2.5  # scipy 1.18 requires numpy>=2.0 (Day 27 QA: env mismatch broke model loading); <2.5 matches feature-store pin
python-dotenv
pyarrow
scikit-learn
lightgbm  # Day 12 — production model (was missing from requirements!)
keras  # Day 15 LSTM baseline (runs on torch backend, no TensorFlow)

# --- Serving layer (Render deployment, Days 17-23) ---
streamlit
fastapi
uvicorn
plotly
streamlit-js-eval  # browser geolocation
shap  # Day 20 explainability
pydeck  # Global AQI Map (deck.gl renderer for st.pydeck_chart)
prometheus-client  # Issue #41: cache metrics (hit/miss counters, size gauge)
slowapi==0.1.9     # Issue #40: API rate limiting (per-endpoint, per-IP)
# NOTE: hopsworks is deliberately NOT here — it pins protobuf<5.0.0 and
# pandas<2.4.0, which conflict with keras (protobuf>=5.26.1) and pandas 3.x.
# It lives in requirements-feature-store.txt, installed in a SEPARATE venv
# for the feature-pipeline scripts (see that file + feature_pipeline.yml).
