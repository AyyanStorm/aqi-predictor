# Python cache
__pycache__/
*.py[cod]

# Virtual Environment
venv/
.venv/
.venv-test/
.venv-doc/

# Environment variables
.env

# PyCharm
.idea/

# Logs
*.log

# Build files
build/
dist/
*.egg-info/
*.egg/
*.whl

# Jupyter
.ipynb_checkpoints/

# OS files
.DS_Store
Thumbs.db
*.swp
*.swo
*~
.DS_Store?
.AppleDouble
.LSOverride

# Data & models — belong in Feature Store / Model Registry, never in git.
#
# Plan B (no Hopsworks yet): the Parquet feature store and the joblib
# model registry ARE the persistent state now, and they live IN the repo
# so the GitHub Actions pipelines can share them across runs. Everything
# else under data/ stays ignored.
#
# `data/*` (not `data/`) so the dirs below can be re-included; the Plan B
# negations must come LAST — later lines win in gitignore.
data/*
*.csv
*.parquet
*.pkl
*.joblib
*.h5

# --- Plan B: feature store + model registry live in the repo ---
!data/processed/
!data/processed/feature_store_parquet/
!data/processed/feature_store_parquet/*.parquet
!data/models/
!data/models/registry/
!data/models/registry/**

# HTTP response cache
.cache.sqlite
*.sqlite
.cache*.sqlite
.prediction_cache.json

# pip-compile cache
.pip-tools-cache/

# Test cache
.pytest_cache/
.mypy_cache/
.coverage
coverage.xml
htmlcov/

# VS Code
.vscode/

# Streamlit production secrets
.streamlit/secrets.toml

# OpenClaw agent workspace (not project code)
AGENTS.md
SOUL.md
IDENTITY.md
USER.md
TOOLS.md
HEARTBEAT.md
memory/
media/
skills/
.clawhub/
openclaw-workspace-state.json

# IDE & Editor files
.vscode/
.idea/
*.sublime-project
*.sublime-workspace

# Environment files (except examples)
.env.local
.env.*.local
.env.grafana  # Runtime config with credentials - keep example only

# Temporary files
*.tmp
*.bak
*.backup
*.temp

# Node modules (if any frontend deps added)
node_modules/

# Mac specific
.AppleDouble
.LSOverride

# Windows specific
desktop.ini
$RECYCLE.BIN/

# IntelliJ
*.iml
*.iws
*.ipr

# Vim
.vim/
.swp
.swo
