# 2026-08-12

## Timezone + local-time display for the AQI dashboard (Day 24 scope)

Implemented ONLY timezone/time-display (no AQI tracking / accuracy / DB — later).

Decisions (grill-me Q1 → Option A):
- **Live ticking clock**: `@st.fragment(run_every=30)` around the 4 AQI cards. Key gotcha: fragment args are frozen on capture, so `horizon_times()` is computed INSIDE the fragment body from just the tz name. Context-manager form (`with st.fragment(...)`) is NOT supported on Streamlit 1.61.1 — decorator form only.
- **Horizon semantics**: absolute elapsed hours (`now + timedelta(hours=24/48/72)` in the city's zone, DST-correct via zoneinfo), matching "when 24h from now are completed".

New/changed:
- `src/utils/geo.py`: `resolve_timezone(lat, lon, name=None)` — geocoding name-first (returns tz field, cached), fallback to forecast API `timezone=auto` for any lat/lon (covers quick-pick/GPS/IP).
- `src/utils/local_time.py` (new, pure Python): `horizon_times()`, `format_local_dt()` ("Aug 12, 2026 — 10:10 AM"), `tz_display_name()`.
- `app/components/location_picker.py`: all 4 paths now store `"timezone"` in the location dict (search takes it from geocode result directly).
- `app/components/forecast_cards.py`: `_cards_fragment(tz)` ticking fragment + time footer in each card + tz caption.
- `app/streamlit_app.py`: passes `loc` to `render_forecast_cards(result, loc)`.

Verified with Streamlit AppTest: 0 exceptions, Karachi → Asia/Karachi · UTC+5, cards show Current/+24h/+48h/+72h at local times (e.g. Aug 12 → 13 → 14 → 15, 10:10 AM). All 5 spec cities resolve correctly (Dubai→Asia/Dubai, London→Europe/London, NY→America/New_York, Tokyo→Asia/Tokyo).

Note: `use_container_width` deprecation warnings are pre-existing (app-wide), not from this change.

## Bugfix: Windows crash (reported by AYYAN from PyCharm on Windows)

`ValueError: Invalid format string` at `local_time.py` `format_local_dt()`:
- Root cause: `%-d` in strftime is a **GNU/Linux-only extension**. Windows Python raises ValueError. My Linux tests passed → missed it.
- Fix: build day from `ts.day` (portable). Also added **tzdata** to requirements.txt — Windows Python ships no IANA tz database, so `ZoneInfo()` would raise ZoneInfoNotFoundError there (silent "Local times unavailable").
- Verified: formatter output identical; tzdata makes zoneinfo work with empty PYTHONTZPATH (Windows simulation); full AppTest regression 0 exceptions. Committed+merged as 1a186ea.

## Hopsworks client install on Windows — the real blockers (06:36)

User's base Python is **3.14** (cp314 wheels in pip log). The `imp` error was a
decoy: pip backtracks 5.0.4 → 3.7.0 → 3.4.4 → 3.4.3 because:
1. hopsworks 5.0.4 (ONLY version with a wheel) requires Python <3.14 → rejected on 3.14.
2. 3.4.3's setup.py uses `imp` (removed in Py3.12+) → build crash.
3. Even on supported Python, hopsworks pulls pyjks→twofish; twofish ships NO wheels
   (sdist only, needs C compiler) → fails on Windows without MSVC.

Verified on my Linux box: hopsworks 5.0.4 NEVER imports pyjks at runtime →
`pip install "hopsworks==5.0.4" --no-deps` + manual deps minus pyjks → import OK.
requirements-feature-store.txt now: pins hopsworks>=5.0.4,<6, documents Python<3.14,
and the Windows --no-deps workaround. User must recreate .venv-hs with Python 3.12/3.13.

## Hopsworks login on Windows: /tmp cert folder crash (09:35)

Auth SUCCEEDED (got past login) but crashed at cert materialization:
`FileNotFoundError: [WinError 3] ... '/tmp\\eu-west.cloud.hopsworks.ai'` — the
client defaults cert_folder to "/tmp" (Linux path) and os.mkdir fails on Windows.
Fix: pass `cert_folder=os.path.join(tempfile.gettempdir(), "hopsworks_certs")`
to hopsworks.login() in feature_store.py _connect(). Verified with a mocked
hopsworks module that the kwarg flows through correctly.

## AQI tracking + accuracy system (grill-me: C / auto / per-browser) — merged bafac50

Grill-me decisions (all user-confirmed):
- Q1 formula: Option C — MAPE-based headline accuracy (max(0, 100−|p−a|/max(a,1)·100)),
  ±15 AQI tolerance hit-rate, EPA category-match as supporting stats
- Q2: actuals fetched RETROACTIVELY from Open-Meteo archive at view time (no
  background collection); predictions stored in Hopsworks `aqi_predictions` FG
  (HUDI, auto-create, same adapter pattern) + Parquet fallback
- Q3: AUTOMATIC tracking — every generated prediction saved (idempotent per
  city+base-hour), no opt-in button
- Q4: per-browser anonymous user_id via localStorage (streamlit-js-eval
  get/set_local_storage), fallback session uuid; user_id filter column

New files: src/tracking/{__init__,store,accuracy}.py, app/components/accuracy.py.
streamlit_app.py: after forecast cards -> get_user_id + maybe_save_prediction +
render_accuracy, all wrapped in try/except so tracking never breaks forecast UI.

UI: "📊 Prediction vs Actual AQI" chart (amber predicted line+markers at
current/+24/+48/+72 city-local times, blue actual hourly line, plotly_dark,
#0e1117 bg) + Option-C metric cards; "🎯 Your Average Accuracy" section with
headline % + cities tracked / predictions saved / tracking period / correct counts.
Empty/loading/in-progress states via st.info/st.caption. Track My City reuses
existing location picker (GPS/IP/search) — no duplicate.

Verified: accuracy math unit tests, store upsert round-trip, full AppTest
0 exceptions, existing sections intact (subheaders list unchanged + new ones).
# 2026-08-12

## Timezone + local-time display for the AQI dashboard (Day 24 scope)

Implemented ONLY timezone/time-display (no AQI tracking / accuracy / DB — later).

Decisions (grill-me Q1 → Option A):
- **Live ticking clock**: `@st.fragment(run_every=30)` around the 4 AQI cards. Key gotcha: fragment args are frozen on capture, so `horizon_times()` is computed INSIDE the fragment body from just the tz name. Context-manager form (`with st.fragment(...)`) is NOT supported on Streamlit 1.61.1 — decorator form only.
- **Horizon semantics**: absolute elapsed hours (`now + timedelta(hours=24/48/72)` in the city's zone, DST-correct via zoneinfo), matching "when 24h from now are completed".

New/changed:
- `src/utils/geo.py`: `resolve_timezone(lat, lon, name=None)` — geocoding name-first (returns tz field, cached), fallback to forecast API `timezone=auto` for any lat/lon (covers quick-pick/GPS/IP).
- `src/utils/local_time.py` (new, pure Python): `horizon_times()`, `format_local_dt()` ("Aug 12, 2026 — 10:10 AM"), `tz_display_name()`.
- `app/components/location_picker.py`: all 4 paths now store `"timezone"` in the location dict (search takes it from geocode result directly).
- `app/components/forecast_cards.py`: `_cards_fragment(tz)` ticking fragment + time footer in each card + tz caption.
- `app/streamlit_app.py`: passes `loc` to `render_forecast_cards(result, loc)`.

Verified with Streamlit AppTest: 0 exceptions, Karachi → Asia/Karachi · UTC+5, cards show Current/+24h/+48h/+72h at local times (e.g. Aug 12 → 13 → 14 → 15, 10:10 AM). All 5 spec cities resolve correctly (Dubai→Asia/Dubai, London→Europe/London, NY→America/New_York, Tokyo→Asia/Tokyo).

Note: `use_container_width` deprecation warnings are pre-existing (app-wide), not from this change.

## Bugfix: Windows crash (reported by AYYAN from PyCharm on Windows)

`ValueError: Invalid format string` at `local_time.py` `format_local_dt()`:
- Root cause: `%-d` in strftime is a **GNU/Linux-only extension**. Windows Python raises ValueError. My Linux tests passed → missed it.
- Fix: build day from `ts.day` (portable). Also added **tzdata** to requirements.txt — Windows Python ships no IANA tz database, so `ZoneInfo()` would raise ZoneInfoNotFoundError there (silent "Local times unavailable").
- Verified: formatter output identical; tzdata makes zoneinfo work with empty PYTHONTZPATH (Windows simulation); full AppTest regression 0 exceptions. Committed+merged as 1a186ea.

## Hopsworks client install on Windows — the real blockers (06:36)

User's base Python is **3.14** (cp314 wheels in pip log). The `imp` error was a
decoy: pip backtracks 5.0.4 → 3.7.0 → 3.4.4 → 3.4.3 because:
1. hopsworks 5.0.4 (ONLY version with a wheel) requires Python <3.14 → rejected on 3.14.
2. 3.4.3's setup.py uses `imp` (removed in Py3.12+) → build crash.
3. Even on supported Python, hopsworks pulls pyjks→twofish; twofish ships NO wheels
   (sdist only, needs C compiler) → fails on Windows without MSVC.

Verified on my Linux box: hopsworks 5.0.4 NEVER imports pyjks at runtime →
`pip install "hopsworks==5.0.4" --no-deps` + manual deps minus pyjks → import OK.
requirements-feature-store.txt now: pins hopsworks>=5.0.4,<6, documents Python<3.14,
and the Windows --no-deps workaround. User must recreate .venv-hs with Python 3.12/3.13.

## Hopsworks login on Windows: /tmp cert folder crash (09:35)

Auth SUCCEEDED (got past login) but crashed at cert materialization:
`FileNotFoundError: [WinError 3] ... '/tmp\\eu-west.cloud.hopsworks.ai'` — the
client defaults cert_folder to "/tmp" (Linux path) and os.mkdir fails on Windows.
Fix: pass `cert_folder=os.path.join(tempfile.gettempdir(), "hopsworks_certs")`
to hopsworks.login() in feature_store.py _connect(). Verified with a mocked
hopsworks module that the kwarg flows through correctly.

## Full Hopsworks loop LIVE (11:06–11:26) — 3 workflows green

User asked to complete the loop: training must read FROM Hopsworks, not Parquet.
- requirements-training.txt (new): `-r requirements-feature-store.txt` + lightgbm/
  scikit-learn/joblib (hopsworks + ML stack; keras deliberately absent — protobuf conflict).
- training_pipeline.yml: now installs requirements-training.txt (was requirements.txt);
  secrets already passed → get_feature_store() picks Hopsworks when installed.
- backfill_pipeline.yml (new): workflow_dispatch, seeds FULL history into Hopsworks.
- Backfill result: **351,360 rows written to Hopsworks 'aqi_features'** (verified
  "Verification result : PASS"). Cosmetic failure on last step (`git add data/raw/`
  is gitignored → exit 1) → fixed persist step to only add data/processed/feature_store_parquet/.
- Training run: **Read 351600 rows from Hopsworks** → lgbm_v6 promoted to production,
  RMSE **24.07** (was 27.16) — big win from 4y history vs 120d Parquet.
- GitHub secrets (AyyanStorm/aqi-predictor): HOPSWORKS_PROJECT/HOST/PORT set by me via
  gh CLI (auth'd as tahirhsn91); HOPSWORKS_API_KEY added by user manually (never
  in chat after the earlier leak warning — user regenerated).
- Cron live: feature_pipeline hourly (`5 * * * *`), training_pipeline daily 06:30 PKT,
  both verified green including scheduled runs.

## AQI tracking + accuracy system (grill-me: C / auto / per-browser) — merged bafac50

Grill-me decisions (all user-confirmed):
- Q1 formula: Option C — MAPE-based headline accuracy (max(0, 100−|p−a|/max(a,1)·100)),
  ±15 AQI tolerance hit-rate, EPA category-match as supporting stats
- Q2: actuals fetched RETROACTIVELY from Open-Meteo archive at view time (no
  background collection); predictions stored in Hopsworks `aqi_predictions` FG
  (HUDI, auto-create, same adapter pattern) + Parquet fallback
- Q3: AUTOMATIC tracking — every generated prediction saved (idempotent per
  city+base-hour), no opt-in button
- Q4: per-browser anonymous user_id via localStorage (streamlit-js-eval
  get/set_local_storage), fallback session uuid; user_id filter column

New files: src/tracking/{__init__,store,accuracy}.py, app/components/accuracy.py.
streamlit_app.py: after forecast cards -> get_user_id + maybe_save_prediction +
render_accuracy, all wrapped in try/except so tracking never breaks forecast UI.

UI: "📊 Prediction vs Actual AQI" chart (amber predicted line+markers at
current/+24/+48/+72 city-local times, blue actual hourly line, plotly_dark,
#0e1117 bg) + Option-C metric cards; "🎯 Your Average Accuracy" section with
headline % + cities tracked / predictions saved / tracking period / correct counts.
Empty/loading/in-progress states via st.info/st.caption. Track My City reuses
existing location picker (GPS/IP/search) — no duplicate.

Verified: accuracy math unit tests, store upsert round-trip, full AppTest
0 exceptions, existing sections intact (subheaders list unchanged + new ones).

## Tracking "No tracked predictions" debugging (16:17–16:38) — 2 bugs, 2 fixes

Symptom: user saw graph on first search, later visits show "No tracked predictions
for this city yet" + "Your Average Accuracy — No tracked predictions yet".

Bug 1 (root cause, fixed e79a0ff): per-browser user_id via streamlit-js-eval
localStorage. On a FRESH page load the JS bridge hasn't rendered yet →
get_local_storage() returns None EVEN IF an id is stored → old code minted a NEW
uuid and OVERWROTE localStorage → previous predictions orphaned under the old id.
Fix: rerun once (st.rerun() guarded by `_user_id_retried` session flag) so the
bridge delivers the stored value, then ADOPT it; only create a new id when a second
read still returns None (genuinely fresh browser). ALSO moved get_user_id() OUTSIDE
the try/except in streamlit_app — RerunException must propagate, not be swallowed.

Bug 2 (silent save failure, fixed ac924be): get_prediction_store() factory picks
Hopsworks when HOPSWORKS_API_KEY+PROJECT set AND hopsworks importable. If the
dashboard is run from `.venv-hs` (has hopsworks + real .env creds), the tracking
store becomes HopsworksPredictionStore → fg.insert() CRASHES on Windows (the
jks/pyjks/twofish wall from 09:35-ish) → exception caught+logged → UI silently
shows "no tracked predictions". My server AppTest proved the pipeline itself is
correct (1 row saved to Parquet, graph renders, 0 exceptions) — env issue on
user's machine. Fix: `_resolve_store()` cached in st.session_state, tries
Hopsworks, falls back to ParquetPredictionStore on ANY failure, shows a visible
st.warning when the fallback is used; save+load use the SAME store instance
within a session.

Instructions given to user (awaiting confirmation): git pull (gets e79a0ff +
ac924be), stop app, `streamlit run app/streamlit_app.py`, HARD-refresh browser
(Ctrl+Shift+R), predictions live in data/tracking/predictions.parquet
(gitignored → NOT pushed; Render deploy would be ephemeral unless storage added —
future consideration). Caveat told: pre-fix prediction orphaned, everything after
persists. Also asked them to confirm PyCharm interpreter is `.venv` (streamlit)
not `.venv-hs`.
